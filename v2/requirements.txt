# CMF Schedule v2
openpyxl>=3.1            # workbook import (phase 1), Excel export (phase 6)
Pillow>=10.0             # REQUIRED by openpyxl to read images. Without it
                         # openpyxl silently returns zero images and the import
                         # "succeeds" with no screenshots at all.
psycopg[binary]>=3.1     # Postgres driver
psycopg_pool>=3.2        # connection pooling
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9  # form posts

# dev
pytest>=8.0
httpx2>=0.28              # required by fastapi/starlette TestClient
